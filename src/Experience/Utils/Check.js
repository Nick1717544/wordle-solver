function includesMultipleLetters(word = 'string', letters = 'string')
{
    const lettersSplit = letters.split('') // o, t

    if (lettersSplit.length == 1)
    {
        if (word.includes(lettersSplit[0]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 2)
    {
        if (word.includes(lettersSplit[0]) && word.includes(lettersSplit[1])) // word = about
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 3)
    {
        if (word.includes(lettersSplit[0]) && word.includes(lettersSplit[1]) && word.includes(lettersSplit[2]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 4)
    {
        if (word.includes(lettersSplit[0]) && word.includes(lettersSplit[1]) && word.includes(lettersSplit[2]) && word.includes(lettersSplit[3]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 5)
    {
        if (word.includes(lettersSplit[0]) && word.includes(lettersSplit[1]) && word.includes(lettersSplit[2]) && word.includes(lettersSplit[3]) && word.includes(lettersSplit[4]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 6)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 7)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 8)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 9)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 10)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 11)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 12)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 13)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 14)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 15)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 16)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 17)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 18)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 19)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17])
        && word.includes(lettersSplit[18]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 20)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17])
        && word.includes(lettersSplit[18])
        && word.includes(lettersSplit[19]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 21)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17])
        && word.includes(lettersSplit[18])
        && word.includes(lettersSplit[19])
        && word.includes(lettersSplit[20]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 22)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17])
        && word.includes(lettersSplit[18])
        && word.includes(lettersSplit[19])
        && word.includes(lettersSplit[20])
        && word.includes(lettersSplit[21]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 23)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17])
        && word.includes(lettersSplit[18])
        && word.includes(lettersSplit[19])
        && word.includes(lettersSplit[20])
        && word.includes(lettersSplit[21])
        && word.includes(lettersSplit[22]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 24)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17])
        && word.includes(lettersSplit[18])
        && word.includes(lettersSplit[19])
        && word.includes(lettersSplit[20])
        && word.includes(lettersSplit[21])
        && word.includes(lettersSplit[22])
        && word.includes(lettersSplit[23]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 25)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17])
        && word.includes(lettersSplit[18])
        && word.includes(lettersSplit[19])
        && word.includes(lettersSplit[20])
        && word.includes(lettersSplit[21])
        && word.includes(lettersSplit[22])
        && word.includes(lettersSplit[23])
        && word.includes(lettersSplit[24]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 26)
    {
        if (word.includes(lettersSplit[0])
        && word.includes(lettersSplit[1])
        && word.includes(lettersSplit[2])
        && word.includes(lettersSplit[3])
        && word.includes(lettersSplit[4])
        && word.includes(lettersSplit[5])
        && word.includes(lettersSplit[6])
        && word.includes(lettersSplit[7])
        && word.includes(lettersSplit[8])
        && word.includes(lettersSplit[9])
        && word.includes(lettersSplit[10])
        && word.includes(lettersSplit[11])
        && word.includes(lettersSplit[12])
        && word.includes(lettersSplit[13])
        && word.includes(lettersSplit[14])
        && word.includes(lettersSplit[15])
        && word.includes(lettersSplit[16])
        && word.includes(lettersSplit[17])
        && word.includes(lettersSplit[18])
        && word.includes(lettersSplit[19])
        && word.includes(lettersSplit[20])
        && word.includes(lettersSplit[21])
        && word.includes(lettersSplit[22])
        && word.includes(lettersSplit[23])
        && word.includes(lettersSplit[24])
        && word.includes(lettersSplit[25]))
        {
            return true
        } else {return false}
    }
}

function doesntIncludeLetters(word = 'string', letters = 'string')
{
    const lettersSplit = letters.split('')

    if (lettersSplit.length == 1)
    {
        if (!word.includes(lettersSplit[0]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 2)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 3)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 4)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 5)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 6)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 7)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 8)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 9)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 10)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 11)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 12)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 13)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 14)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 15)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 16)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 17)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 18)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 19)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17])
        && !word.includes(lettersSplit[18]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 20)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17])
        && !word.includes(lettersSplit[18])
        && !word.includes(lettersSplit[19]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 21)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17])
        && !word.includes(lettersSplit[18])
        && !word.includes(lettersSplit[19])
        && !word.includes(lettersSplit[20]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 22)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17])
        && !word.includes(lettersSplit[18])
        && !word.includes(lettersSplit[19])
        && !word.includes(lettersSplit[20])
        && !word.includes(lettersSplit[21]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 23)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17])
        && !word.includes(lettersSplit[18])
        && !word.includes(lettersSplit[19])
        && !word.includes(lettersSplit[20])
        && !word.includes(lettersSplit[21])
        && !word.includes(lettersSplit[22]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 24)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17])
        && !word.includes(lettersSplit[18])
        && !word.includes(lettersSplit[19])
        && !word.includes(lettersSplit[20])
        && !word.includes(lettersSplit[21])
        && !word.includes(lettersSplit[22])
        && !word.includes(lettersSplit[23]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 25)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17])
        && !word.includes(lettersSplit[18])
        && !word.includes(lettersSplit[19])
        && !word.includes(lettersSplit[20])
        && !word.includes(lettersSplit[21])
        && !word.includes(lettersSplit[22])
        && !word.includes(lettersSplit[23])
        && !word.includes(lettersSplit[24]))
        {
            return true
        } else {return false}
    }
    if (lettersSplit.length == 26)
    {
        if (!word.includes(lettersSplit[0])
        && !word.includes(lettersSplit[1])
        && !word.includes(lettersSplit[2])
        && !word.includes(lettersSplit[3])
        && !word.includes(lettersSplit[4])
        && !word.includes(lettersSplit[5])
        && !word.includes(lettersSplit[6])
        && !word.includes(lettersSplit[7])
        && !word.includes(lettersSplit[8])
        && !word.includes(lettersSplit[9])
        && !word.includes(lettersSplit[10])
        && !word.includes(lettersSplit[11])
        && !word.includes(lettersSplit[12])
        && !word.includes(lettersSplit[13])
        && !word.includes(lettersSplit[14])
        && !word.includes(lettersSplit[15])
        && !word.includes(lettersSplit[16])
        && !word.includes(lettersSplit[17])
        && !word.includes(lettersSplit[18])
        && !word.includes(lettersSplit[19])
        && !word.includes(lettersSplit[20])
        && !word.includes(lettersSplit[21])
        && !word.includes(lettersSplit[22])
        && !word.includes(lettersSplit[23])
        && !word.includes(lettersSplit[24])
        && !word.includes(lettersSplit[25]))
        {
            return true
        } else {return false}
    }
}

export { includesMultipleLetters, doesntIncludeLetters }